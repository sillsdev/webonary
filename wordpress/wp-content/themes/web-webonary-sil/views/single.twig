{% extends "base.twig" %}

{% block content %}
	
<section id="content_primary">

  <div class="container">
  	<div class="row">
  	
  		<div class="col-md-8 col-sm-8">
    		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
    			<section class="article-content">
    				<h1 class="article-h1">
    				  {{post.title}}
    				</h1>
    				<div class="article-body">
						<!--
    					<p class="postmeta">
					      <a href="{{post.author.path}}"> {{ post.author.name }} </a>| {{ post.display_date }}
				      </p> -->
              {{post.content}}
    				</div>
    			</section>
    			
<!--        	<section class="comments">
    				<div class="respond">
    					<h3 class="h2">Comments</h3>
    						{{ comment_form }}
    					</div>
    					<div class="responses">
    
    						{% for cmt in post.get_comments() %}
    							{% include "comment.twig" with {comment:cmt} %}
    						{% endfor %}
    					</div>
    
    			</section>-->
    			
    		</article>
      </div>
      
{#      {% include "sidebar.twig" %}#}
      
    </div>
  </div>

</section>


{% endblock %}